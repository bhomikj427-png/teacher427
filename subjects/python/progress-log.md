# Python — Progress Log

> Written **live** as sessions happen (not reconstructed at wrap-up). Session-resume block is
> overwritten each session-end; Session history is append-only. See `../README.md`.

## Session resume
- Last session: 2026-06-16 (subject setup + full research build + complete deep dive / backlog clear)
- Where we stopped: subject created; knowledge base **fully built and fully verified** (map +
  exhaustive deep pass, units 01–14; 15 misconceptions; sources; changelog; curriculum); **goal
  locked = resume-grade professional**, **version pinned = Python 3.14**; to-verify backlog
  **cleared** (all deferred items resolved at primary source); **no teaching yet**.
- Recap (what happened):
  - Created the **Python** subject; built the KB to `subject-research-protocol.md` standard.
  - Verified the load-bearing spine against **primary sources** (Language Reference §3/§4, Library
    Reference, glossary, FAQ; PEPs 20/8/484; Batchelder) — not from recall.
  - Exhaustive deep pass: per-unit notes 01–14; 15 sourced misconceptions; tiered sources.
  - **Complete deep dive (2nd instruction):** locked goal = resume-grade professional, pinned target
    = Python 3.14, and **closed the entire to-verify backlog** at primary source — numeric ops/bool,
    copy/deepcopy, import system, data-model methods (`__new__`/`__init__`/`__repr__`/`__eq__`/
    `__hash__`), MRO/`super`, name mangling, exceptions + groups, functools/contextlib/PEP257,
    version-gated syntax (`match`/`X|Y`/generics/`type`), free-threading (3.13 exp → 3.14 supported),
    packaging (venv/pip/pyproject/uv). All M1–M15 now primary-sourced.
  - Only irreducible item left: learner's **prior programming exposure** (session-1 diagnosis).
  - Did **not** yet teach.
- Next up: **Session 1** — (a) probe prior programming exposure (the one open variable); (b) begin
  **U1** (orientation: run code, seed "names point at objects"), then into **U2/U3** at the diagnosed
  level. **U3 (T1, names-bind-to-objects)** is the first ★★ threshold — budget time.
- Due for review today: nothing yet (no items mastered).

## Mastery ledger
*(empty — nothing mastered to criterion yet)*

## Spaced-review / relearning queue
*(empty — fills as items are mastered)*

## Session history
- [2026-06-16] Subject created (level = absolute zero). Researched & wrote the knowledge base: map
  pass + **exhaustive deep pass** (per-unit notes 01–14), misconceptions (15), tiered sources,
  changelog, curriculum. Load-bearing spine verified at primary python.org sources + canonical PEPs.
  No teaching yet.
- [2026-06-16] **Goal locked = resume-grade professional; version pinned = Python 3.14.** Ran the
  **complete deep dive**: closed the entire to-verify backlog at primary source (numeric/bool, copy,
  imports, data-model methods, MRO/super, name mangling, exceptions+groups, functools/contextlib/
  PEP257, version-gated syntax, free-threading 3.13→3.14, packaging). All M1–M15 primary-sourced;
  curriculum/profile/log updated. Only open item: learner's prior-programming exposure. No teaching
  yet. Next: Session 1 — probe prior experience, then U1.
- [2026-06-16] **KB integrity audit** (learner-directed: "find cracks in research"). Content spine
  independently re-checked → **no factual errors** (floor-division, bool⊂int, `is`/`==` interning,
  mutable-default, LEGB, `super()`/MRO, name-mangling, `__eq__`/`__hash__`, all PEP/version numbers
  correct). Found + fixed **annotation drift**: records claimed the verify-backlog 100% closed while
  ~13 unit notes still said "page-verify at pass," incl. 3 self-contradictions (`02`/`11`/`12` headers
  said "queued" though their bodies were already `[verified 2026-06-16]`). Resolved self-contradictions
  in the verified direction; relabeled the rest (standard/uncontested facts) to honest status. KB now
  carries no live verify-later residue. Logged in `knowledge-base/CHANGELOG.md`. Still no teaching.
