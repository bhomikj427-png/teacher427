# ECE2120 — Exam map (marks-aiming) — ⚠ STUB, `uncertain`

> The marks-aiming map: per topic → exam frequency, recurring question types + marks, MTE/ETE split,
> instructor emphasis from PPTs. Built from `../exam-pack/`. **Status: `uncertain` — no PYQ/PPT in
> hand yet** (`exam-pack/` is empty). Until populated, priority below is inferred from the topic
> structure and the standard weighting of these topics in network-theory courses, **not** from real
> MUJ exam evidence. See `../../research-engine/exam-resources.md`.

## Exam structure
- MTE + ETE (integrated-lab subject, L-T-P-C 3-0-2-4). Exact weightage **to-verify** (not confirmed).

## Provisional priority (INFERRED, not from PYQs — replace once exam-pack is filled)
- **High (expected heavy, numerical):** Thévenin/Norton equivalents incl. **dependent sources**
  (test-source method); second-order transient response (RLC: over/critical/under-damped, find
  i(t)/v(t)); Laplace solution of a switched circuit with initial conditions; two-port **parameter
  computation** (Z/Y/h/ABCD of a given network) and **interconversion**.
- **Medium:** superposition & max-power-transfer numericals; initial/final-value problems; ABCD of a
  cascade; **Foster/Cauer synthesis** of a given LC/RC/RL function; testing a polynomial for Hurwitz.
- **Lower / short (theory):** statement/proof of theorems; properties of PR functions; image
  impedance; symmetric T/π & filter constants; driving-point vs transfer-function definitions.

## To-verify (route to `00-map.md` register)
- `[opened 2026-06-22]` **No ECE2120 PYQ/PPT ingested** — download MTE+ETE papers + course PPTs into
  `../exam-pack/` (via `../../_inbox/`), then rebuild this map from real evidence (topic frequency,
  exact question types, marks). *Resolve by:* manual drop from MUJ Central/Stella/Toppers.
- `[opened 2026-06-22]` **MTE/ETE mark weightage** unverified.
