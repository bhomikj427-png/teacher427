# ECE2120 Circuits & Network Theory — CHANGELOG (correction audit trail)

> Format (`subject-research-protocol.md §9`):
> `[YYYY-MM-DD] <claim/topic> — <what changed> — <why> — <trigger> — <confidence: old → new>`
> Never silently overwrite a claim — log the correction here.

- `[2026-06-22]` **Subject KB created → Stage 1 built** — added `00-map.md`, `exam-map.md` (uncertain
  stub), units `01`–`05`, `misconceptions.md`, `sources.md` — built to exam-ready (Stage-1) depth from
  the prescribed texts, with all high-risk exact claims verified against external sources this session
  — trigger: learner activated the subject for teaching ("complete kb for circuit and network theory")
  — confidence: n/a → per-unit `settled` (load-bearing specifics verified) except exam-weighting
  `uncertain`.
- `[2026-06-22]` **Synthesis realizability conditions (PR / Hurwitz / RC-RL-LC / Foster-Cauer)** —
  verified before writing, not recalled — RC nearest-origin-pole vs RL nearest-origin-zero, PR's five
  testable conditions, Cauer I/II ladder topologies, AC conjugate match — why: these are the exact
  claims most easily mis-stated on recall (§5) — trigger: pre-write verification pass — confidence:
  recall-`uncertain` → `settled` (triangulated, `sources.md`).
- `[2026-06-22]` **Teaching order vs syllabus order** — recorded that U4 (network functions) is taught
  **before** U3 (two-ports) and U5 (synthesis) despite the syllabus listing two-ports first — why: the
  pole-zero / PR language of U4 is a hard prerequisite for U3 parameters-as-functions-of-s and U5
  realizability — trigger: prereq-graph construction (map pass, §8) — confidence: `settled` (the
  syllabus list is not a dependency claim).
