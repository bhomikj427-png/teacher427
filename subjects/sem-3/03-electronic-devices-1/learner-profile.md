# ECE2101 Electronic Devices-I — Learner Profile

> Current level, goals, known/observed misconceptions, and subject-specific preference overrides.
> Maintained by the engine via the self-update protocol (`../../../CLAUDE.md`).
> Scaffolded 2026-07-05 (engine audit — subject is `stage-2✓`/TEACH-READY); **level undiagnosed —
> first session opens with the diagnostic, per protocol.**

## Goal
- MUJ sem-3 core: score on MTE + ETE (Stage-1 set first, scoring-over-depth), then Stage-2 depth —
  this subject is substrate for the VLSI/RTL career track (MOSFET → CMOS → digital → verilog).

## Current level — **undiagnosed** (diagnose at first session, re-diagnose every answer)
- Prior signal available: Digital Electronics stage-2 KB work implies exposure to CMOS/logic, and
  the verilog subject implies digital-logic context — but device *physics* level is unknown.
- First-session probes: drift vs diffusion (U1 threshold), what the depletion region is (U2
  threshold), diode I-V shape and why. Route by result (novice → worked examples; developing →
  guided practice).

## Known / suspected misconceptions to watch
*(from `knowledge-base/misconceptions.md` M1–M25 — confirm via retrieval, don't assume)*
- "Current = drift only" (Ohm's-law instinct; misses diffusion) — the U1 threshold. High-priority probe.
- Vbi measurable with a voltmeter / no field without a battery — U2 threshold.
- Transistor as "magic amplifier" instead of a controlled source — U5 gateway.
- Small-signal used without a Q-point (large/small-signal conflation).

## Preferences (overrides)
*(Subject-specific only; win over `../../../learner-preferences.md` inside this subject. None yet.)*

## Tensions to manage
- None yet.
