# MAS2001 Statistics & Probability — Learner Profile

> Current level, goals, known/observed misconceptions, and subject-specific preference overrides.
> Maintained by the engine via the self-update protocol (`../../../CLAUDE.md`).

## Goal
- **Depth over exam-cramming.** `[stated 2026-06-19]` No exam date, no official PYQ/PPT material in
  hand, so the target is **genuine understanding** rather than recipe-memorisation. Underlying intent
  is still "score well" — depth is the chosen route there. → teach for transfer (why, not just how);
  Stage-2 depth is in-scope when a unit warrants it, not just Stage-1 exam minimum.
- Re-scope the moment real exam material (handout / past papers) arrives — `00-map.md` open items
  (unit boundaries, PYQs) still stand. See [[source-priority-rule]].

## Current level — **novice in statistics, developing in basic probability** (re-diagnose each session)
- **Stated:** "I know the very basics of both, but being honest even the very basics of stats is
  weak." `[stated 2026-06-19]`
- **Probability:** simple counting confirmed — got P(exactly one head in two fair flips) = ½ correct
  from first principles, and flagged it as trivial. → don't pitch at counting-trivia level; find the
  real edge (conditional probability / complement / RV-as-function is the likely break point).
- **Math prerequisites — load-bearing:** "comfortable with summation, same with integration, but
  **can't handle complex-looking equations** in either." `[stated 2026-06-19]` → **the dominant
  constraint on delivery.** One new symbol at a time; build every expression up piece-by-piece;
  concrete numbers before abstract notation; never present a dense formula cold. This is extraneous-
  load management (`research/01 §2`), not a ceiling on ability.
- Entry point: curriculum **U1** (probability & random variables → E/Var → CLT).

## Known / suspected misconceptions to watch
*(from `knowledge-base/misconceptions.md` — confirm or rule out via retrieval, don't assume)*
- **★ Sampling distribution of a statistic** (M4/M5 per map) — the master threshold; budget extra
  time when U1's CLT and U3/U4 arrive.
- **★ CLT as the bridge** — why normality appears; why "n large" rescues inference.
- **★ CI / p-value meaning** (M6/M8–M10) — threshold-level misreadings, surface in U3/U4.
- Early-probability slips to watch as we open U1: confusing P(A and B) with P(A or B); treating
  dependent events as independent; conditional-probability direction (P(A|B) vs P(B|A)).

## Preferences (overrides)
*(Subject-specific only; these win over `../../../learner-preferences.md` inside this subject.)*
- [observed 2026-06-19] **Dislikes trivially-easy probes; reads them as wasted time** ("too
  rudimentary… I know all this"). → calibrate diagnostics *up* to the ~80% edge; don't re-probe
  confirmed-easy ground. (Serves the calibration principle anyway — note it's *observed*, revisable.)
- [stated 2026-06-19] Keep equations from looking complex. → see Current level; deliver math in
  small built-up steps. *(Delivery of the load principle, not an override of it.)*

## Tensions to manage
- None yet. (Watch: "aim for depth" + "can't handle complex equations" can pull against each other —
  resolve by reaching depth through *built-up* notation and worked examples, never by dumping the
  dense form.)
