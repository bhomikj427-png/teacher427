# Address Sequencing ｜｜ Microprogram Sequencer ｜｜ Computer Organization Architecture
# source: https://youtu.be/nxNytEYQLKI
# AUTO-CAPTION — verify formulas/numbers against textbook before teaching

in this video we are going to discuss about address sequencing or can also be called as microprogram sequencer generally we will design control unit by using two approaches the first approach is heart rate control unit the second approach is micro programmed control unit so here we are here we are going to discuss about the micro programmer control unit in micro program control unit the control logic is implemented with the help of a software a program so that micro program is stored in control memory so control memory contains control memory contains the micro program which is useful for executing the control unit next every micro program contains a collection of micro instructions and each micro instruction contains one or more micro operations so what is control memory here control memory stores micro program micro program contains a collection of micro instructions and micro instruction contains a collection of micro operations so that's why here we have written micro operations here so why cpu is executing the current instruction address then we have to generate the next instruction address so those two should be done simultaneously so while the cpu is executing the first instruction then how we can generate the next instruction address so that is nothing but the concept address sequencing so how we can generate the next instruction address sequence means how we will get the next instruction address our micro program sequencer so the duty of micro program sequencer is to generate the next micro instruction address okay here there are four approaches are there the first approach is the first approach is incrementing incrementing control address register by one here we have control address register control address register contains address of micro instruction that is to be stored in the control memory okay so let me have a control memory like this so let the first instruction is triple zero let the data of this instruction is one zero one zero like that let the second instruction address is double zero one variables let the data is like this zero one one zero one like that so control address register contains the instruction what is the micro instruction address here this triple zero so control address register contains the micro instruction address that is to be stored in the control memory okay so here the faster approach is incrementing control address register by one so for that purpose here we are using an incremented circuit so increment our circuit increments control address register by one so what is the previous value in control letters register triple zero if we increment triple zero by one then the output is double zero one so now with the help of the multiplexer uh we know about the multiplexer okay so multiplexer accepts that value and it will transfers that value to control address register so once incrementation is over then the multiplexer transfers double zero one to the control address register so now the next instruction address will become double zero one so that is the first control approach incrementing register by one and now let's see the second approach the second approach is subroutine call and return sub protein resistor sub protein register we know what is a subroutine subroutine is nothing but a function that we use as in c c plus plus and java any programming language a subroutine contains a collection of instructions which are useful in order to perform a specific task mainly we use a subroutine for the reputation purpose so here we know that whenever a function is called then the control goes to the function definition all the statements in the function definition will be executed once the execution is over then the control comes to the control comes to the next statement of the main program so that next statement will be stored in subroutine register subroutine register let in the main program we are at the address four thousand five hundred so what is the next instruction four thousand five hundred finance one so this four thousand fine art one will be stored in subroutine register after that the control goes to the subroutine so all the statements in the subroutine will be executed and after that based upon this subroutine register value control address resistor will be get will be generated okay here what is the subroutine resistor here 4501 so multiplexer transfers that four five zero one to the control address register so now now uh four five zero one uh instruction will be executed now so let it be some four five zero one instruction address so now this instruction will gets executed so this is the second way using subroutine resistor now let's see the third way third way is unconditional the third way is unconditional or conditional branch so what is the target unconditional or conditional branch based upon the status bits so first let's see about unconditional branch unconditional means the name itself specifies the meaning unconditional means without checking the condition the corresponding address will be loaded into the control address register so let us assume that here we have an address like this that we are executing that the cpu is executing double zero one whereas here left address is like this triple one is the address if it is unconditional address then what will happen is without checking any condition here the condition won't be checked here what is the branch address here triple one so this triple one will be transferred to the control address register with the help of the multiplexer okay so here what is the branch address 31 so without checking any without checking any condition uh this triple one will be transferred to the control address register with the help of the multiplexer now the control address register contains tribal one so now the instruction at the address 311 will gets executed this is about unconditional unconditional means there is no need to check any condition directly we can jump and the next one is conditional branch conditional branch means we have to check the condition whenever the condition is satisfied then only we have to load that address into the control address register here for that purpose here we have to select the status bit uh here the branch logic check the status bit there are several status bits are there like 0 flag sign bit flag overflow flag likewise there are several bits are there let us assume that now the flag is for zero zero flag so we need to check whether the result is zero or not here let the result is zero whereas let the instruction address is something like this one zero one zero let the instruction address is like this okay so the branch logic will checks whether here the content is zero or not so here we have content as zero so so what uh generally control memory contents for some micro instruction in micro instruction we will have some conditional bits conditional bit branch bit status bit likewise we have several bits in the micro instruction and at the condition bit is triple zero here so this triple zero will be checked here so here the condition bit is zero only so very good the condition is satisfied so this address this one zero one zero address will be loaded into the control address register with the help of the multiplexer so this is about conditional branch based upon the status bits so branch logic here we have branch logic branch logic will checks whether that status condition is met or not if that status condition is satisfied then only the corresponding address will be loaded into the control address register suppose if the condition is false let the content is double zero one here but we are checking for zero flag here so we can say that this is false in that occasion increment or increments control address register by one so the next instruction address will be executed so this is the third approach now let's see the fourth approach here the fourth approach is external mapping mapping the instruction fourth approach is mapping instruction mapping instruction so for that purpose here we are using some instruction register uh let us assume that this is our instruction register so the first four bits are some one zero double bar and this is nothing but some address field here we will do some mapping here on opcode output this is nothing but output operation which we are performing so on this op code we will do some mapping so on those four bits we will do some magic so that's why i'm using uh some don't gets this excess specifies that we are doing some mapping here uh here the mapping is add one zero to the left side and we add two zeros to the right side this is our mapping it is our choice we need to generate some instruction address that's it so zero so we need to copy these four bits here one zero double bond and we are adding uh these two zeros here so the next instruction address is this address zero one zero double bond double zero so this address will be so here we have mapping logic so this is nothing but mapping logic mapping logic means if the mapping logic is append one zero at the left side two zeros at the right side and copy the output bits as it is okay this is the mapping logic so that after that multiplexer transfers the corresponding address to control address register so now now uh the inspection at this address will gets executed okay so in this way we can generate the next instruction address why its previous instruction is execute so this is about address sequencing or micro program sequencer in the next video we will discuss