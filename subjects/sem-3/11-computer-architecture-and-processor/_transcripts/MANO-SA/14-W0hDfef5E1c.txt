# Input Output Instructions ｜｜ Computer Organization
# source: https://youtu.be/W0hDfef5E1c
# AUTO-CAPTION — verify formulas/numbers against textbook before teaching

in this video we are going to discuss about input/output instructions first let's see how the data will be transmitted from input devices to the output images the most commonly used input device this keyboard so whenever we type any character with the help of the keyboard then the transmitter interface transness then we have transmitted interface transmitted interface transmits the character to a register called again Priya I am Priya stands for input resisted yet dia at the input resistor a flag called efg is maintained we have the I stands for input flag we have G a value will be set to 2 1 if input register contains a character various if the input resistor doesn't contains any character then the FGA will be clear to 0 now if G a value is fun why because issues that we have typed a a character and that character presencing input resistor okay next year input resistor passes that character to the accumulator input resistor passes that character to the accumulator next we have a out resistor out registered stands for output resistor output register receives information from accumulator near the output which is there a flag called the FG who is maintained yet the output is still a flag order here what a accumulator is doing accumulator is receiving information from the input resistor and passing that information to the output register here yes G was transferred out to flag the initial value of the F 0 is fun if output resistant container a character then FG will be cleaner to 0 so it is reversed to thee yes GN if we have T is equal to 1 then it specifies that input resistor contains a character whereas if FG i is equal to 0 then we can say that input resistance does not contains any contact various here output is disturb mix initially FG value is one y FG y is equal to one means output register doesn't contains any character various FG y is equal to zero means output register contains a character so output register receives a character from the accumulator and receiver interface we have receiver interface receiver interface and receives that character from the output register and provides that character to the monitor so that the character bigots printed on the monitor so in this set the transmission will be done here we know how to represent the instruction format the size of the instruction format is 15 bits in this the fasted fallible specifies IO operations the first elaborate specifies i/o operations and our code triple one specifies the operation to be performed and here the address mode bitties II's fun in their equities fun so what are the opcode bits when up orbits are removeth then i bow operations are to be executed so we can say that our instructions are executed when the decoders output d7 is active d7 means this 3 1 in binary notation 4 to 1 in binary notation means 7 so then the decoders output d7 is active then i go instructions are executed and here body is a value either u-visa so that is represented by next year T naught T 1 T 2 timing signal specifies fetching and decoding of the inspections whereas the next turning signal is DT so input/output instructions are operations to be executed when d7i T 3 is active when decoders output d7 and indirect bit I as well as timing signal T 3 is active let it be represented by P so d7 I T 3 is nothing but P so very ask the instructions be lemon what is the instruction here this is nothing but xi pdp-11 performs the first instruction P we can perform the second instruction so likewise we have six instructions pb11 performs one inspection PB 10 performs one instruction like that the inspections we hit now let us discuss those instructions one by one the fast inspection is impr a young people the fast inspection is a MV int stands for input it imports a character to the accumulator so the contents of the input resistor will be passed to the accumulator so the contents off input resistor will be transferred to the accumulator but here the problem is the size of the input register is 8 bits various the size of the accumulator is 16 bits so for the most significant bits of accumulate depth the data will be transferred so that ADA will be transferred so the most eighth significance of the accumulator so from 0 to 7 so for those bits the data will be transferred the least significant bits from 15 to 8 will be making a 0 again next year next year input flag here input resistor doesn't contains any data why because the data is transferred to the accumulator so you have GA value will begin u FD a is equal to g so it specifies that the input register doesn't contains any data next one is out so out stands for output output a character to the accumulator okay saltines what the accumulator out that output register gets a character from the accumulator ok if output register gets a character from the accumulator so here output register is receiving the character from the accumulator but what is the size of the output register a bits so here the most 8 significant bits of the accumulator are to be transferred to the output register ok now what is the M 0 + 0 + VG why because output register contains data if output register contains data we have given value in 0 if output register doesn't contain any data then F 0 value is 1 and the next one is steep on input flap yes ki ka stands for skip an input rap that means if we have GI value is fun if input lag is one then skip the next instruction skipping the next instruction is nothing but increment the program counter BAM so that the next instruction board gets executed next one is steeper output flat that means if output flag is one then if the next instruction so how to skip the next instruction by implementing the program contract so that next instruction wound gets executed next one is ie ie n stands for interrupt enabled an indirect variable on so that so here the inspection is I go yet interrupt enable on so what we have it this idea will be set to two one so whenever this ie and will be set it over then interrupt flag will be set to work and the next one is interrupt off interrupt off so whenever a here is here to 0 then the interrupt will be off so by handling the interrupt related instructions we will use these two so these are the various input output instructions which are available in the computer