# Arithmetic Logic Shift Unit ｜｜ALU ｜｜  One Stage Of Arithmetic Logic Shift Unit｜Computer Organization
# source: https://youtu.be/LjuIcbqRrbI
# AUTO-CAPTION — verify formulas/numbers against textbook before teaching

Now let's see about arithmetic logic shift unit. Uh in short, we can call it as ALU. Uh arithmetic logic shift unit performs arithmetic micro operations, uh logic micro operations, and shift micro operations. Uh in the previous videos, we have seen about how to implement arithmetic micro operations with the help of a circuit. Next, we have seen uh logical micro operations hardware implementation, as well as shift micro operations implementation. That is nothing but a 4-bit combinational circuit shifter. Uh so, three different videos we have seen. Uh whereas in this concept, what we are doing is we are combining all those three hardware implementations, such as arithmetic micro operations hardware implementation, logical micro operation hardware implementation, and shift micro operations implementation into a single circuit. That circuit is called as arithmetic logic shift unit. So, arithmetic logic shift unit performs all the arithmetic operations, logic micro operations, as well as shift micro operations. Now let's see this diagram. Uh here we have a 4x1 multiplexer. Uh 4x1 multiplexer means uh we know that it accepts four inputs and produces one of the input as the output. So, 4x1 multiplexer. Uh if it is a 4x1 multiplexer, uh then the selection inputs are two. Why because if there are n selection inputs, then 2 ^ n data lines, then two power data inputs are needed. So, here yes three and yes three are nothing but the selection inputs which are to be applied to the multiplexer. So, there are two selection inputs are there. So, totally we can have two power two inputs. So, 0 1 2 3 are nothing but inputs of the multiplexer. Here, zero receives input from the output of the arithmetic circuit. So, that means if you select 0 0, then it performs arithmetic micro-operation. Next, one input receives data from one stage of logic circuit. So, one input receives data from logical circuit. So, if you select 0 1, then it performs logical micro-operations. Next, two data input receives data from AI - 1. Here, it is a bit. Here, we are using this bit in order to perform shift right operation. Next, three input receives data from AI + 1, which performs shift left operation. Here, the point is very very simple. Here, the multiplexer is accepting four inputs. If you select 0 0, then arithmetic micro-operations will be performed. If you select 0 1, then logical micro-operations will be performed. If you select 1 0, then shift right operation will gets performed. If you select 1 1, then shift left operation will be performed. Here, we are using the selection inputs S3 and S2 in order to control this multiplexer. Now, let's see these boxes. So, this box is nothing but one stage of arithmetic circuit. Uh so, already we have seen about arithmetic circuit diagram. Uh so, in that diagram, totally we have taken a register Just we have taken a register which contains four bits. So, totally four bits are taken. A0, A1, A2, A3, B0, B1, B2, B3, and some carries, C0, C1, C2, C3, like that. But, in this In this circuit, what we have taken is we have taken only the single bit. Only we have taken only the first bit. Uh let the first register content is like this. Uh A0 A3 A2, A1, A0. Let the second register content is B3, B2, B1, B0. Let the initial carry is C0. So, here we have taken only A0, B0, and C0. Let us assume that we are performing some addition operation. So, after performing addition, we may get some sum value. As well as we will get some carry. So, let the carry is C1. So, let's see this diagram. This is nothing but one stage of an arithmetic circuit which performs addition operation or any operation on only the corresponding bits, A0, B0, C0, A1, B1, C1, like that. Okay? Here, CI is nothing but input carry to the arithmetic circuit. So, this zero C0 is nothing but here the input carry. So, if you perform any arithmetic operation, then we will get some output carry. Here, the output carry is nothing but CI + 1. That CI + 1 will be passed here as input to the next stage of the arithmetic circuit. So, what is the next stage of the arithmetic circuit? Here, this arithmetic circuit is performing operations on A0, B0, C0. So, if we add these three numbers, we will get some carry. So, that carry value will be passed to the next stage of the arithmetic circuit. So, the next stage will performs operations on A1, B1, C1. Then, we will get some sum value. Next, we will get some carry. So, that carry value will be passed here as input to the next stage of the arithmetic circuit. So, if you want to perform operations on four bits, then we have to repeat this one for four times. We have to repeat this stage for four times. Okay? So, this is nothing but arithmetic circuit. Uh so, very very simple. So, here arithmetic circuit is accepting some input carry, and it is producing some output carry CI + 1. This will be passed here as input to the next stage of the arithmetic circuit. And the output of the arithmetic circuit is DI. The DI is passed here as input to the zero input of the multiplexer. So, if you select zero zero, then what will happen? This zero input uh output will be produced. So what is the zero input? Zero input is getting data from output of the arithmetic circuit. So arithmetic operations will get performed. Now let's see this one. This is nothing but one stage of the logic circuit. Uh so the output of here also we are using some selection inputs S1 and S0. Uh S1 and S0 controls arithmetic circuit as well as logic circuit. So this is nothing but S1. This is nothing but S0. So S1 and S0 are the selection inputs which are to be applied to the arithmetic circuit as well as logic circuit. Uh so here also the logic based upon the operations, the logic circuit will perform the corresponding operation and it produces the output. That output is denoted by EI. So EI is passed as input to the data input one of the multiplexer. Okay? So output of the logic circuit is EI. So that EI is passed as input to the input one of the multiplexer. Here for the logical circuit, we know that there is no need of any carry. So there is no need to specify any carry for the corresponding logic circuit. Uh we know that we have mainly four logic operations are there. Logic and and operation, or operation, exclusive or operation, and complement operation. Those operations are controlled with S1 and S0. So if S1 and S0 is 00, then perform some and operation. If S1 and S0 is 01, then perform some or operation. Like that. Okay? So here S1 and S0 is controlling arithmetic circuit as well as logic circuit. Whereas S3 and S2 selection inputs are controlling the multiplexer. Next, what are AI and BI? AI is nothing but register A bit. BI is nothing but register B bit. If you take this example, 0101. 1001. So this is nothing but AI. This is nothing but BI. So, A0B0. So, that information we are passing to logic circuit as well as arithmetic circuit. Okay? Here we are passing only these these two bits. So, once operation is performed on these two bits, then these two bits will be passed. These 00 will be passed. Next, this 10 will be passed. So, for that we are using this one. AI represents one bit of register A. BI represents one bit of register B. Next, AI minus one. AI minus one represents a bit which is useful in order to perform shift to right operation. Next, AI plus one represents a bit which is useful in order to perform shift to left operation. Okay? So, here the naming conventions is also very very simple. A and B are nothing but register content. So, we are passing AI and BI as inputs to arithmetic circuit as well as logic circuit. Next, AI minus one is a bit which performs shift to right operation. AI plus one represents a bit which is needed to perform shift to left operation. So, A is over, B is over. Next, CI represents input carry. CI plus one represents output carry. So, C is over. Next, D represents output of the arithmetic circuit. E represents output of the logic circuit. F represents the output of the multiplexer. Okay? So, which operation we are performing? That is nothing but F. Okay? Uh now, let us analyze this diagram. If the selection inputs are 00, selection inputs means that these these inputs. If yes three and yes two are 00, then which operation will gets performed? Arithmetic operation. So, if yes three yes two are 00, then arithmetic operation will gets performed. Here totally there are eight arithmetic operations are there. okay? So, that's why this we have we have to write eight zero zeros. If arithmetic operations are zero zero, then arithmetic operations will gets performed. 1 2 3 4 5 6 7 8. So, we will write all these operations by taking arithmetic circuit diagram, okay? Next, if the selection inputs if yes three yes two are zero one, if yes three yes two are zero one, then what will happen? This one data input will be selected. What is one data input? Output of the logic circuit. So, that means if the selection inputs are zero one, then logical micro operations will gets performed. Totally there are four logic micro operations are there. So, zero one four zero one four logic micro operations zero one zero one, okay? Let let us write the logic micro operations. So, if the selection if yes three yes two are zero one, then logic micro operations will gets performed. Next, this logic circuit is controlled by yes one and yes not. If yes one and yes not are zero zero, then perform under operation. If yes one and yes not is zero one, then perform or operation. If yes one yes not are one zero, then perform exclusive or operation. If yes one and yes not are one one, then perform complement operation. These four are the simple logic micro operations. By using these operations we can implement any other operation, okay? So, let us write those operations. Yes three yes two zero one means logical operations will gets performed. So, what are yes one and yes not? If yes one and yes not are zero zero, then which operation will be performed? Under operation will be performed. Whereas if yes one and yes not are zero one, then or operation will gets performed. One zero, then XR operation will get performed. Next 1 1 means complement operation will get performed, okay? Next, what is carry value? In order to perform operations on the logical micro operations, there is no need of any carry. Carry is needed only while performing the arithmetic operations. So, just use into mark. That into mark represents don't care. That means we are not using the carry. So, logic micro operation means we are not using any carry, okay? Next, if we have S3 S2 are 1 0, then which micro operation will get performed? This two data input will be selected. Two is getting data from shift right operation. So, if S3 and S2 are 1 0, then shift right micro operation will get performed. Here, in order to perform the shift right micro operation, there is no need of the selection inputs. These selection inputs controls only arithmetic operations and logic micro operations, okay? While performing shift right and shift left, there is no need of this S1 and S2, as well as there is no need of this carry. So, write don't cares. So, this don't care specifies that we are not using selection these two selection inputs, as well as this carry value. Next, if S3 S2 are 1 1, then which operation we are performing? Input three data will be selected. So, that means we are performing shift left operation. Shift left operation, okay? So, just use don't cares. That means we are not using S1 S0 and this input carry value. So, with this logical micro operations are over, as well as uh shift micro operations are over. So, now let's see about arithmetic micro operations. So, already we have seen arithmetic micro operation circuit, but here we have to write those values also. So, for that let us see in brief. So, we have Let we have uh two multiplexers. So 4 by 1 multiplexer, 4 by 1 multiplexer. So likewise generally we will take four multiplexers. Next in order to perform the addition we use this sum fully added. So likewise we use this fully added. So this is nothing but X, this is nothing but Y. Here X gets data from register A. Y gets data from output of the multiplexer. S1 and S0 are nothing but selection inputs of the multiplexer. S1 and S0 are nothing but these are nothing but selection inputs of the multiplexer. So here 4 by 1 multiplexer means we will have four inputs. Zero input receives data from register B. Whereas one input receives data from complement of B. Okay. So likewise zero input receives data from B. One input receives data from complement of B. So likewise for the remaining two multiplexers also same. So let it be some B1. Next this two and three gets data from zero. This two and three gets data from zero. I'm sorry, this two data input gets data from zero. Whereas three data input gets data from one. Zero complement means what? One. So here the point is very very simple. This is nothing but arithmetic circuit. So here we have four selection in four two selection inputs. S1 and S0 are nothing but selection inputs. Zero, one, two, three are nothing but data inputs. Here zero is data input zero is getting data from register B. Next data input one is getting data from complement of B. Next two data input is getting data from zero. Three data input is getting data from complement of zero. Complement of zero means one, okay? Now, based on this we have to perform the operations. Let us assume that the selection inputs are zero zero. Selection inputs are zero zero. If the selection Let the selection inputs are zero. And here the carry value may be either zero or one. So, here a carry will be passed to the corresponding full adder. So, carry value may be either zero or one. Now, let's see what is the operation which gets performed. Let this output is denoted by some G. So, this Z is equal to Here the formula for the output is X is getting data from A. A plus Next, Y means directly it is getting data from output of the multiplexer. Y plus C in. This is nothing but input carry. So, what is A here? A means it is directly getting the data. Here also we have some X and Y. X receives data from A. Y receives data from output of the multiplexer, okay? So, here A is getting the data directly. Next, what is Y here? So, Y means output of the multiplexer. So, output of the multiplexer. Here, what are the selection inputs? Zero zero are the selection inputs. If zero zero are the selection inputs, then zero data input data will be selected and this data will be produced as the output. Here, zero is getting data from which register? B register. So, here Y means output of B. So, B plus So, first what is carry value? C is nothing but carry value. So, that is nothing but here the operation is A plus B. A plus B is nothing but what? Addition operation we are performing. Next, same selection inputs, but the carry value is one. So, here the C in is one. So, the operation which we are performing is A plus B plus one. So, this is nothing but addition only. Addition with carry, but here we have one here. Next, let's take the selection inputs 1 0. Uh here the carry value may be either 0 or 1. Now, let us calculate here. If the selection inputs are 0 1, then what will happen? One input data will be selected and the data will be passed as input to the Y. So, what is Y input data? Y is getting data from complement of B. So, here the Y value is complement of B means B bar. Plus what is the initial value? Here the carry value is 0. A plus B bar plus 0. A plus B bar plus 0 is nothing but A plus B bar. This operation is called as subtraction with borrow. This operation is called as subtraction with borrow. Next one, carry value is 1. So, carry value is 1. A plus B bar plus 1. This is nothing but two's complement of B. So, this operation is nothing but subtraction operation. We know how to subtract the two binary numbers by adding two's complement of the second register to the first register. So, this is nothing but A minus B. So, what is A minus B? A minus B is nothing but subtraction operation. Now, let us consider about the next situation, that is 0 0 is 1, I'm sorry. This is 0 1. This is 0 1. 0 1 means one data input will be selected. So, next combination 1 0. 1 0. Here the carry values may be either 0 or 1. So, if 1 0 if the selection inputs are 1 0, then two data input will be selected. Here two is getting data from 0. So, the 0 will be passed as input to the Y. So, A plus 0 plus what is the carry value? 0. That is nothing but A. So, A means what? Here the operation is transfer operation. So, transfer A. Next, the carry value is 1. Carry value is 1. So, that means this is nothing but increment operation. So, increment A. Next, the last combination. 1 1. So, here the carry value may be either zero or one. Okay. Now let's see here. Okay. If the inputs are 11, then three data input will be selected. What is three data input? It is getting data from complement of zero. That is nothing but one. So here plus it is getting data from one. So we will get all ones. If the if there are four registers, then we will get four ones. So here plus one plus what is the carry value here? Zero is the carry value. So this is nothing but A plus one. A plus one is nothing but we are performing the operation A minus one. Why because what is A minus one? A minus one means we have to calculate two's complement of one and that should be added to A. So how to calculate two's complement of one? For that first we have to represent one in binary notation. 3001. Next to calculate two's complement. So two's complement means first we have to calculate one's complement. So 3110. Next we have to add one. So all ones. Here what we are doing? Here we are adding four ones. If there are four bits, then we we have to add those four bits. So this is nothing but A minus one. A minus one means what? Decrement A. Next we have to add the carry. So if you add carry to A minus one, then what is the output? A minus one plus one, that is nothing but A. So this is nothing but transfer A operation. So these are the eight plus four plus two. So eight arithmetic operations, four logic micro operations, and two shift operations. We can perform all those all those these operations with the help of this arithmetic logic shift unit. So by using these operations, we can perform the remaining all other operations.